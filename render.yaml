services:
  - type: web
    name: receiptify-frontend
    env: static
    buildCommand: npm install && npm run build --configuration=production
    staticPublishPath: ./dist/frontend
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
